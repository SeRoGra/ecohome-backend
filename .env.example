# ─────────────────────────────────────────────────────
# EcoHome Backend — Variables de entorno
# Copiar este archivo a .env y ajustar los valores
# NUNCA subir .env al repositorio (está en .gitignore)
# ─────────────────────────────────────────────────────

# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ecohome
DB_USER=postgres
DB_PASS=cambia_esta_clave

# JWT
# Debe ser una cadena de al menos 32 caracteres
JWT_SECRET=4RCGcUXGMszaPRV2EjEsEtsupnInRNZcxeAFacqpUI8=
JWT_EXPIRATION_MS=86400000   # 24 horas en ms
